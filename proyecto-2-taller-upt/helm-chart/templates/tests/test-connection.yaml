apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "poc-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
